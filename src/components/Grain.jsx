import './Grain.css';

/** A fixed, very low-opacity film grain that keeps the flat grounds from looking digital-clean. */
export default function Grain() {
  return <div className="grain" aria-hidden="true" />;
}
